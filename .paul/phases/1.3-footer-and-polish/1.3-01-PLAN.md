---
phase: 1.3-footer-and-polish
plan: 01
type: execute
wave: 1
depends_on: ["1.2-01"]
files_modified: [src/components/Footer.jsx, src/components/Footer.module.css, src/App.jsx, src/pages/PrivacyPolicy.jsx, src/pages/TermsOfService.jsx, src/pages/About.jsx, src/pages/Contact.jsx, src/styles/legal.css]
autonomous: true
---

<objective>
## Goal
Build the Footer component with links to legal pages (Privacy Policy, Terms of Service, About, Contact) and create placeholder legal template pages. Footer appears at bottom of all views with copyright, links, and creator attribution.

## Purpose
Phase 1.2 built the interactive calendar. Phase 1.3 completes the MVP with legal compliance (privacy, terms), branding (about, contact), and user trust signals (footer). This is the final layer before mobile testing and deployment.

## Output
- `src/components/Footer.jsx` + `.module.css` — sticky footer with legal links, copyright, attribution
- `src/pages/PrivacyPolicy.jsx` — Privacy Policy template (placeholder)
- `src/pages/TermsOfService.jsx` — Terms of Service template (placeholder)
- `src/pages/About.jsx` — About Cyber Talks page
- `src/pages/Contact.jsx` — Contact page with email
- `src/styles/legal.css` — Shared styles for legal pages
- `src/App.jsx` (enhanced) — Route legal pages, wrap with ErrorBoundary
</objective>

<context>
## Project Context
@.paul/PROJECT.md
@.paul/ROADMAP.md
@.paul/STATE.md

## Prior Work
@.paul/phases/1.2-navigation-and-integration/1.2-01-SUMMARY.md
(Provides: CalendarHeader, BottomSheet, ErrorBoundary, query param routing, analytics)

## Source Files
@src/App.jsx
@src/components/DayView.jsx
@src/components/ThreeDayView.jsx
@src/styles/global.css
</context>

<acceptance_criteria>

## AC-1: Footer Component with Legal Links
```gherkin
Given the footer appears at bottom of all pages
When viewing any view (Day, 3-Day, or legal pages)
Then footer shows copyright "© 2026 Cyber Talks"
And footer displays links: "Privacy Policy", "Terms of Service", "About", "Contact"
And links are clickable and navigate to respective pages
And footer background is #1a1a2e with text #e0e0e0
And footer has cyan border on top (#00f5ff)
And footer is sticky/fixed at bottom (does not scroll away on mobile)
And footer is responsive (single row on desktop, stacked on mobile if needed)
```

## AC-2: Privacy Policy Page
```gherkin
Given user clicks "Privacy Policy" link in footer
When the page loads
Then it displays privacy policy placeholder content
And content includes sections: Data Collection, Cookies, Third-party Services, Contact
And page has back button or breadcrumb to return to calendar
And page has same header/footer as main calendar (consistent branding)
And content is readable on mobile (proper line length, font size)
```

## AC-3: Terms of Service Page
```gherkin
Given user clicks "Terms of Service" link in footer
When the page loads
Then it displays terms of service placeholder content
And content includes sections: Use License, Disclaimers, Limitation of Liability, Contact
And page has back button or breadcrumb to return to calendar
And page has same header/footer as main calendar
And content is mobile-readable
```

## AC-4: About Page
```gherkin
Given user clicks "About" link in footer
When the page loads
Then it displays information about Cyber Talks
And content mentions: Daily TikTok live stream, Cybersecurity guests, Creator name (Nate Butler)
And content links to creator TikTok: @natebutlerexplains
And page has navigation to return to calendar
And page has same header/footer as main calendar
```

## AC-5: Contact Page
```gherkin
Given user clicks "Contact" link in footer
When the page loads
Then it displays contact information
And content includes: Email address, TikTok handle, GitHub link
And page has clear call-to-action (e.g., "Email", "Follow on TikTok")
And links open in new tab (external links)
And page has navigation to return to calendar
And page has same header/footer as main calendar
```

## AC-6: App Routing for Legal Pages
```gherkin
Given the app has legal pages
When user clicks footer link
Then the URL changes (e.g., /privacy, /terms, /about, /contact)
And the page renders without errors
And browser back/forward works correctly
And footer appears on all pages consistently
```

</acceptance_criteria>

<tasks>

<task type="auto">
  <name>Create Footer component with legal links</name>
  <files>src/components/Footer.jsx, src/components/Footer.module.css</files>
  <action>
    Create Footer.jsx:
    - Accept optional props: onNavigate (callback for page navigation, use React Router Link or similar)
    - Render sticky footer at bottom of viewport
    - Left section: Copyright "© 2026 Cyber Talks" + "Nate Butler"
    - Center section: Links to Privacy, Terms, About, Contact (use React Router Link or anchor tags)
    - Right section: Optional social links or additional attribution
    - Style: background #1a1a2e, text #e0e0e0, border-top 2px #00f5ff
    - Make responsive: single row on desktop, can wrap on mobile if needed
    - Ensure footer doesn't overlap content (use flexbox or padding)
    
    Create Footer.module.css:
    - Footer container: flex, justify-content: space-between
    - Links: cursor pointer, hover effect (cyan color glow)
    - Responsive: on mobile (<640px), center text and stack sections vertically
    Avoid: Footer overlapping content, broken links, hardcoded routing
  </action>
  <verify>Render Footer on all pages, check links are clickable, verify styling matches spec</verify>
  <done>AC-1 satisfied: Footer with legal links and proper styling</done>
</task>

<task type="auto">
  <name>Create legal page templates and routing</name>
  <files>src/pages/PrivacyPolicy.jsx, src/pages/TermsOfService.jsx, src/pages/About.jsx, src/pages/Contact.jsx, src/styles/legal.css, src/App.jsx</files>
  <action>
    Create src/pages/ directory if not exists.
    
    Create PrivacyPolicy.jsx:
    - Functional component with standard privacy policy template
    - Sections: Data Collection (Firebase Analytics, no PII), Cookies (PWA service worker), Third-party Services (Firebase, Google Fonts), Contact
    - Include back button or "Return to Calendar" link
    - Wrap in common layout with header/footer
    
    Create TermsOfService.jsx:
    - Functional component with standard terms template
    - Sections: Use License (view only, no commercial use), Disclaimers (guest info owned by creators), Limitation of Liability, Contact
    - Include back button
    
    Create About.jsx:
    - Describe Cyber Talks: daily TikTok live stream with cybersecurity guests
    - Mention creator: Nate Butler (@natebutlerexplains)
    - Link to TikTok with target="_blank" rel="noopener noreferrer"
    - Include back button
    
    Create Contact.jsx:
    - Display contact options: Email (if available), TikTok link, GitHub link
    - All links open in new tab
    - Include back button
    
    Create src/styles/legal.css:
    - Shared styles for legal pages: max-width 800px, centered, readable typography
    - Consistent with global.css theme
    
    Update src/App.jsx:
    - Add React Router or simple routing (can use URL hash or window.location)
    - Routes: /, /privacy, /terms, /about, /contact
    - Render Footer on all pages
    - Render legal pages instead of DayView/ThreeDayView when route matches
    - Use ErrorBoundary to wrap all content
    
    Avoid: Hardcoded legal content (use templates), broken links, missing back navigation
  </action>
  <verify>Navigate to each legal page, check content renders, verify back button works, check footer appears on all pages</verify>
  <done>AC-2, AC-3, AC-4, AC-5, AC-6 satisfied: Legal pages and routing complete</done>
</task>

<task type="auto">
  <name>Integrate Footer into existing DayView and ThreeDayView layouts</name>
  <files>src/components/DayView.jsx, src/components/ThreeDayView.jsx, src/App.jsx</files>
  <action>
    Verify DayView and ThreeDayView render correctly with Footer below content.
    
    In App.jsx:
    - Wrap entire page content in a flex container with min-height 100vh
    - DayView/ThreeDayView/legal pages should take remaining space
    - Footer sits at bottom, always visible
    - Use CSS: body { display: flex; flex-direction: column; min-height: 100vh; }
    
    No modifications to DayView/ThreeDayView structure needed—Footer renders at App level.
    
    Test that footer doesn't overlap calendar content on desktop and mobile.
    Avoid: Footer hiding content, layout shifts on page load
  </action>
  <verify>Render app, scroll content, verify footer stays at bottom, no overlaps</verify>
  <done>Footer integrated into full app layout</done>
</task>

</tasks>

<boundaries>

## DO NOT CHANGE
- src/components/CalendarHeader.jsx (locked from Phase 1.2)
- src/components/BottomSheet.jsx (locked from Phase 1.2)
- src/components/ErrorBoundary.jsx (locked from Phase 1.2)
- src/components/DayView.jsx (read-only)
- src/components/ThreeDayView.jsx (read-only)
- src/styles/global.css (theme locked)
- src/firebase.js (analytics locked)

## SCOPE LIMITS
- Legal page content is PLACEHOLDER - will be refined later by legal team
- No email backend for contact form (just display email address)
- No analytics tracking on legal pages (optional, defer to Phase 2)
- No admin UI for managing legal content (manual edits only)
- Mobile testing is Phase 1.3-02 (not in this plan)

</boundaries>

<verification>
Before declaring plan complete:
- [ ] Footer renders on all pages (calendar, legal pages)
- [ ] All legal links clickable and navigate correctly
- [ ] Back button/return link works on all legal pages
- [ ] Footer styling matches spec (colors, spacing, responsiveness)
- [ ] App routing works (URL changes, browser history works)
- [ ] No console errors in browser
- [ ] `npm run build` completes successfully
- [ ] ErrorBoundary wraps all content
- [ ] All acceptance criteria pass
</verification>

<success_criteria>
- All 3 tasks completed without errors
- All 6 acceptance criteria verified
- Footer appears on every page (calendar + legal)
- Legal pages render with placeholder content
- Routing works (hash-based or URL params)
- Ready for Phase 1.3-02 (mobile testing + performance)
</success_criteria>

<output>
After completion, create `.paul/phases/1.3-footer-and-polish/1.3-01-SUMMARY.md`
</output>
